# Copy to .env and fill in. Base values shared across your own dev setups;
# personal one-off overrides go in .env.local instead (see .env.local.example).

# Two merchant API key (defaults to dummy-dev-key if unset here and in .env.local)
TWO_API_KEY=
